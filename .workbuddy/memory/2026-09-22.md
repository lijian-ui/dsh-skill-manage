# 2026-09-22

## dsh-skill-manage 对齐官方 typert 契约（create）
- 需求：把 `@lijian-ui/dsh-skill-manage` 适配 dsh 官方 monorepo 规范（E:\Project\deepseek-harness）。
- 核心改动：typert codec 契约从 `schema: <zod>` 改为 `create: () => <zod>`（host MANIFEST 与 client CONTRIBUTION 同步）。
  - `src/remote.ts`：加 `import type { TypertRegistry, TypertContribution } from '@deepseek-ai/dsh-typert-registry'`；17 处 codec/result 的 `schema:` → `create: () => `；注册调用已是 `(ctx.get('typert') as TypertRegistry).register(MANIFEST as unknown as TypertContribution)`。
  - `src/client/index.ts`：加 `import type { TypertRemoteContribution } from '@deepseek-ai/dsh-typert-protocol'`；codec helper 改 `create: () => ({ parse: identity })`；CONTRIBUTION 加 `as unknown as TypertRemoteContribution`。
  - `package.json`：devDependencies 补 `@deepseek-ai/dsh-typert-registry`；`dsh.client.inject` 三个包保持 devDependencies（官方/兄弟插件惯例，不移到 peer）。
  - `cordis.patch.yml`：删除冗余空 `config: {}`。
- 风险（用户已确认承担）：官方源码版契约 = `create`，npm 发布版 0.1.6-alpha.1（dsh-desktop 0.5.0 实际装）契约 = `schema`，同名不同实现。改 create 后**破坏 dsh-desktop 0.5.0 兼容**，需升级 dsh 运行时到与新源码一致的版本。
- 未跑 typecheck：本地 node_modules 未装新 dsh 包（用户自行 npm install + 升级 dsh 后验证）。
